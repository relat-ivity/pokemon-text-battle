# Pokemon Console Battle - Environment Variables

# =============================================================================
# 使用说明
# =============================================================================
#
# 1. 复制此文件为 .env
#    cp .env.example .env
#
# 2. 填写你的 API keys
#
# 3. 运行对战
#    npm start
#
# 注意：
# - .env 文件已被添加到 .gitignore，不会被提交到 Git
# - 请勿将包含真实 API key 的 .env 文件分享给他人



# =============================================================================
# DeepSeek AI Configuration (仅用于 DeepSeek AI 对手)
# =============================================================================

# DeepSeek API Key (如果使用 DeepSeek AI 对手)
# 获取 API Key: https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# DeepSeek AI 作弊概率 (可选 - 默认 0.8)
# 设置为 0-1 之间的数值
# 0 = 不作弊，AI 正常异步决策
# 0.5 = 50% 概率成功预判玩家选择 (默认)
# 1 = 必定获得玩家选择信息
DEEPSEEK_CHEAT_PROBABILITY=0.5

# =============================================================================
# Battle Configuration (对战模式配置)
# =============================================================================

# 本地对战格式 (可选 - gen9ou（默认），gen9randombattle)
# 用于 npm start
LOCAL_BATTLE_FORMAT=gen9ou